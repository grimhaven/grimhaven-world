Grimhaven World Data
====================

World data for the Grimhaven MUD (as extracted from the SneezyMUD MySQL dump
files).

JSON format currently. Attempting to denormalize and make more generic than the
existing files.
